// Language configurations live in the client package and are shared verbatim.
// Keeping a byte-identical 700-line copy here meant every sample edit had to be
// made twice, and the two drifted silently when it wasn't.
export * from "../../client/src/config";
